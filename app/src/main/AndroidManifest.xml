<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

        <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Package visibility for OS detection (Android 11+) -->
    <queries>
        <package android:name="app.grapheneos.camera" />
        <package android:name="app.grapheneos.apps" />
        <package android:name="app.grapheneos.dialer" />
        <package android:name="org.calyxos.setup" />
    </queries>

    <application
        android:name=".FialkaApplication"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Fialka"
        android:localeConfig="@xml/locale_config"
        tools:targetApi="31">

        <activity
            android:name=".ui.addcontact.CustomScannerActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:theme="@style/zxing_CaptureTheme" />

        <activity
            android:name=".ui.LockScreenActivity"
            android:exported="false"
            android:filterTouchesWhenObscured="true"
            android:theme="@style/Theme.Fialka" />

        <!-- SplashActivity n'a PLUS de launcher intent-filter direct.
             C'est DisguiseFialkaAlias (enabled=true par défaut) qui est l'entrée
             du lanceur. Ainsi un seul alias est actif à la fois → une seule icône. -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Fialka.Splash" />

        <!-- App Disguise: UN SEUL alias enabled à la fois = une seule icône lanceur -->

        <!-- Alias Fialka par défaut — enabled="true" au démarrage -->
        <activity-alias
            android:name=".ui.disguise.DisguiseFialkaAlias"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".ui.SplashActivity"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".ui.disguise.DisguiseCalculatorAlias"
            android:label="@string/disguise_label_calculator"
            android:icon="@mipmap/ic_launcher_disguise_calc"
            android:targetActivity=".ui.SplashActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".ui.disguise.DisguiseWeatherAlias"
            android:label="@string/disguise_label_weather"
            android:icon="@mipmap/ic_launcher_disguise_weather"
            android:targetActivity=".ui.SplashActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".ui.disguise.DisguiseNotesAlias"
            android:label="@string/disguise_label_notes"
            android:icon="@mipmap/ic_launcher_disguise_notes"
            android:targetActivity=".ui.SplashActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".ui.disguise.DisguiseClockAlias"
            android:label="@string/disguise_label_clock"
            android:icon="@mipmap/ic_launcher_disguise_clock"
            android:targetActivity=".ui.SplashActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Cover Calculator activity — not exported, launched only from SplashActivity routing -->
        <activity
            android:name=".ui.cover.CoverCalculatorActivity"
            android:exported="false"
            android:theme="@style/Theme.Fialka"
            android:excludeFromRecents="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:filterTouchesWhenObscured="true"
            android:theme="@style/Theme.Fialka">
            <!-- Handle fialka://invite deep links from other apps / share sheet -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fialka" android:host="invite" />
            </intent-filter>
            <!-- Handle fialka://mailbox deep links for joining a mailbox -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fialka" android:host="mailbox" />
            </intent-filter>
            <!-- Open .fialka seed backup files from any file manager -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-fialka-backup" />
            </intent-filter>
        </activity>

        <!-- Tor Foreground Service — keeps Tor daemon alive -->
        <service
            android:name=".tor.TorForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Tor anonymity network connection" />
        </service>

        <!-- FileProvider for sharing decrypted files with external apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
