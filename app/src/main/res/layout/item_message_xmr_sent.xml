<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="12dp"
    android:paddingVertical="3dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:orientation="vertical">

        <!-- XMR bubble -->
        <LinearLayout
            android:id="@+id/bubbleXmrSent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_bubble_sent"
            android:backgroundTint="?attr/colorBubbleSent"
            android:orientation="horizontal"
            android:padding="12dp"
            android:gravity="center_vertical"
            android:minWidth="200dp"
            android:maxWidth="300dp">

            <!-- Monero logo -->
            <ImageView
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/ic_monero"
                android:layout_marginEnd="10dp"
                android:importantForAccessibility="no" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvXmrTitleSent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="?attr/colorBubbleSentText"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    android:alpha="0.85" />

                <TextView
                    android:id="@+id/tvXmrAmountSent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="?attr/colorBubbleSentText"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:layout_marginTop="2dp"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/tvXmrNoteSent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="?attr/colorBubbleSentText"
                    android:textSize="13sp"
                    android:layout_marginTop="2dp"
                    android:visibility="gone" />

                <!-- Detail: address or txHash (truncated, tap to copy) -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginTop="6dp">

                    <TextView
                        android:id="@+id/tvXmrDetailSent"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:textColor="?attr/colorBubbleSentText"
                        android:textSize="11sp"
                        android:alpha="0.7"
                        android:maxLines="1"
                        android:ellipsize="middle"
                        android:fontFamily="monospace" />

                    <ImageView
                        android:id="@+id/btnXmrCopySent"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_marginStart="6dp"
                        android:src="@drawable/ic_copy"
                        android:tint="?attr/colorBubbleSentText"
                        android:alpha="0.6"
                        android:clickable="true"
                        android:focusable="true"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/cd_copy" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>

        <!-- Timestamp -->
        <TextView
            android:id="@+id/tvTimeXmrSent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="11sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

</FrameLayout>
